import { Container, Typography } from "@mui/material";

export default function SignUp() {
  return (
    <Container sx={{ py: 4 }}>
      <Typography variant="h4">Sign Up</Typography>
      <Typography>Mock signup page.</Typography>
    </Container>
  );
}
