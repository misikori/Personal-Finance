import { SxProps, Theme } from "@mui/material";

export const convertPanelStyles: { root: SxProps<Theme>; } = {
  root: {
    borderRadius: 3,
    height: "100%",
    // Add more global style references as needed
  },
};
